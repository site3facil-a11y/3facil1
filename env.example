# Copie este arquivo para ".env" e ajuste os valores antes de rodar o projeto
APP_URL=http://localhost:3000

# --- PostgreSQL ---
# Se preencher DATABASE_URL, ela tem prioridade sobre DB_HOST/DB_USER/etc.
DATABASE_URL=
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=3facil_db
DB_USER=tresfacil_app_user
DB_PASSWORD=SuaSenhaSeguraAqui123!

JWT_SECRET=troque-por-um-segredo-aleatorio-longo

# --- SMTP (opcional) ---
SMTP_FROM=
SMTP_HOST=
SMTP_PASS=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
